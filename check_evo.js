const fs = require('fs'); const envFile = fs.readFileSync('.env.local', 'utf8'); const env = {}; envFile.split('\n').forEach(line => { const idx = line.indexOf('='); if (idx > 0) env[line.slice(0,idx).trim()] = line.slice(idx+1).trim().replace(/['"]/g, ''); }); console.log(env.NEXT_PUBLIC_EVOLUTION_API_URL); console.log(env.NEXT_PUBLIC_EVOLUTION_GLOBAL_API_KEY || env.EVOLUTION_API_KEY);
